<!-- AUTO-GENERATED by packages/cli/scripts/generate-skill.ts. -->
<!-- Do not edit by hand. Re-run via `pnpm -F @noya-ai/cli gen`. -->

# `noya chain`

_On-chain operations — transfers, swaps, approvals, lending._

**28 commands** in this group.

## `noya chain compound`

_Compound III lending — supply, withdraw, borrow, repay._

### `noya chain compound borrow`

Auto-generated from sdk.chain.compound.borrow

| Flag | Value | Choices | Description |
| --- | --- | --- | --- |
| `--chain-id` | `<value>` | — | EVM chain ID — e.g. `1` (Ethereum), `8453` (Base), `137` (Polygon), `42161` (Arbitrum). Optional — defaults to `8453` (Base) when omitted. |
| `--asset-id` | `<value>` | — | Compound III asset identifier — typically the symbol like `USDC`, `WETH`, `cbETH`. |
| `--amount` | `<value>` | — | Amount as a string in the token's smallest unit (e.g. `100000000` for 100 USDC at 6 decimals). |

### `noya chain compound get-portfolio`

Auto-generated from sdk.chain.compound.getPortfolio

| Flag | Value | Choices | Description |
| --- | --- | --- | --- |
| `--chain-id` | `<value>` | — | EVM chain ID — e.g. `1` (Ethereum), `8453` (Base), `137` (Polygon), `42161` (Arbitrum). Optional — defaults to `8453` (Base) when omitted. |

### `noya chain compound repay`

Auto-generated from sdk.chain.compound.repay

| Flag | Value | Choices | Description |
| --- | --- | --- | --- |
| `--chain-id` | `<value>` | — | EVM chain ID — e.g. `1` (Ethereum), `8453` (Base), `137` (Polygon), `42161` (Arbitrum). Optional — defaults to `8453` (Base) when omitted. |
| `--asset-id` | `<value>` | — | Compound III asset identifier — typically the symbol like `USDC`, `WETH`, `cbETH`. |
| `--amount` | `<value>` | — | Amount as a string in the token's smallest unit (e.g. `100000000` for 100 USDC at 6 decimals). |

### `noya chain compound supply`

Auto-generated from sdk.chain.compound.supply

| Flag | Value | Choices | Description |
| --- | --- | --- | --- |
| `--chain-id` | `<value>` | — | EVM chain ID — e.g. `1` (Ethereum), `8453` (Base), `137` (Polygon), `42161` (Arbitrum). Optional — defaults to `8453` (Base) when omitted. |
| `--asset-id` | `<value>` | — | Compound III asset identifier — typically the symbol like `USDC`, `WETH`, `cbETH`. |
| `--amount` | `<value>` | — | Amount as a string in the token's smallest unit (e.g. `100000000` for 100 USDC at 6 decimals). |

### `noya chain compound withdraw`

Auto-generated from sdk.chain.compound.withdraw

| Flag | Value | Choices | Description |
| --- | --- | --- | --- |
| `--chain-id` | `<value>` | — | EVM chain ID — e.g. `1` (Ethereum), `8453` (Base), `137` (Polygon), `42161` (Arbitrum). Optional — defaults to `8453` (Base) when omitted. |
| `--asset-id` | `<value>` | — | Compound III asset identifier — typically the symbol like `USDC`, `WETH`, `cbETH`. |
| `--amount` | `<value>` | — | Amount as a string in the token's smallest unit (e.g. `100000000` for 100 USDC at 6 decimals). |

## `noya chain defillama`

_DeFiLlama protocol + token-price reads (chain-agnostic)._

### `noya chain defillama find-protocol`

Auto-generated from sdk.chain.defillama.findProtocol

| Flag | Value | Choices | Description |
| --- | --- | --- | --- |
| `--query` | `<value>` | — | Free-text query to find DeFiLlama protocols by name or slug. |

### `noya chain defillama get-protocol`

Auto-generated from sdk.chain.defillama.getProtocol

| Flag | Value | Choices | Description |
| --- | --- | --- | --- |
| `--protocol-id` | `<value>` | — | DeFiLlama protocol slug (e.g. `aave-v3`, `uniswap`). |

### `noya chain defillama get-token-prices`

Auto-generated from sdk.chain.defillama.getTokenPrices

| Flag | Value | Choices | Description |
| --- | --- | --- | --- |
| `--tokens` | `<value>` | — | Tokens to price, in DeFiLlama's `chain:address` (or `coingecko:id`) format. Example: `["ethereum:0xa0b8..."]`. |

## `noya chain enso`

_Enso router + bridge (cross-chain swaps)._

### `noya chain enso bridge`

Auto-generated from sdk.chain.enso.bridge

| Flag | Value | Choices | Description |
| --- | --- | --- | --- |
| `--chain-id` | `<value>` | — | EVM chain ID — e.g. `1` (Ethereum), `8453` (Base), `137` (Polygon), `42161` (Arbitrum). Optional — defaults to `8453` (Base) when omitted. |
| `--destination-chain-id` | `<value>` | — | Target chain ID for the bridge. |
| `--token-in` | `<value>` | — | Source-chain token address. |
| `--token-out` | `<value>` | — | Destination-chain token address. |
| `--amount-in` | `<value>` | — | Amount in whole token units (e.g. `1.5` for 1.5 ETH). Provider applies the right scaling. |

### `noya chain enso route`

Auto-generated from sdk.chain.enso.route

| Flag | Value | Choices | Description |
| --- | --- | --- | --- |
| `--chain-id` | `<value>` | — | EVM chain ID — e.g. `1` (Ethereum), `8453` (Base), `137` (Polygon), `42161` (Arbitrum). Optional — defaults to `8453` (Base) when omitted. |
| `--token-in` | `<value>` | — | Address of the token to swap from. Use `0xeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeee` for native ETH. |
| `--token-out` | `<value>` | — | Address of the token to swap to. |
| `--amount-in` | `<value>` | — | Amount of `tokenIn` to swap, in whole units (e.g. `100` = 100 USDC). |
| `--slippage` | `<value>` | — | Slippage tolerance in basis points (50 = 0.5%). Defaults to provider's safe value. |

## `noya chain erc20`

_ERC-20 token reads + transfers + approvals._

### `noya chain erc20 approve`

Auto-generated from sdk.chain.erc20.approve

| Flag | Value | Choices | Description |
| --- | --- | --- | --- |
| `--chain-id` | `<value>` | — | EVM chain ID — e.g. `1` (Ethereum), `8453` (Base), `137` (Polygon), `42161` (Arbitrum). Optional — defaults to `8453` (Base) when omitted. |
| `--contract-address` | `<value>` | — | EVM-style 0x-prefixed 40-hex-char address. |
| `--spender` | `<value>` | — | EVM-style 0x-prefixed 40-hex-char address. |
| `--amount` | `<value>` | — | Amount as a string in the token's smallest unit (e.g. `100000000` for 100 USDC at 6 decimals). |

### `noya chain erc20 get-allowance`

Auto-generated from sdk.chain.erc20.getAllowance

| Flag | Value | Choices | Description |
| --- | --- | --- | --- |
| `--chain-id` | `<value>` | — | EVM chain ID — e.g. `1` (Ethereum), `8453` (Base), `137` (Polygon), `42161` (Arbitrum). Optional — defaults to `8453` (Base) when omitted. |
| `--contract-address` | `<value>` | — | EVM-style 0x-prefixed 40-hex-char address. |
| `--spender` | `<value>` | — | EVM-style 0x-prefixed 40-hex-char address. |

### `noya chain erc20 get-balance`

Auto-generated from sdk.chain.erc20.getBalance

| Flag | Value | Choices | Description |
| --- | --- | --- | --- |
| `--chain-id` | `<value>` | — | EVM chain ID — e.g. `1` (Ethereum), `8453` (Base), `137` (Polygon), `42161` (Arbitrum). Optional — defaults to `8453` (Base) when omitted. |
| `--contract-address` | `<value>` | — | EVM-style 0x-prefixed 40-hex-char address. |

### `noya chain erc20 get-erc20-token-address`

Auto-generated from sdk.chain.erc20.getErc20TokenAddress

| Flag | Value | Choices | Description |
| --- | --- | --- | --- |
| `--chain-id` | `<value>` | — | EVM chain ID — e.g. `1` (Ethereum), `8453` (Base), `137` (Polygon), `42161` (Arbitrum). Optional — defaults to `8453` (Base) when omitted. |
| `--symbol` | `<value>` | — | Token symbol to look up (e.g. `USDC`, `WETH`). |

### `noya chain erc20 transfer`

Auto-generated from sdk.chain.erc20.transfer

| Flag | Value | Choices | Description |
| --- | --- | --- | --- |
| `--chain-id` | `<value>` | — | EVM chain ID — e.g. `1` (Ethereum), `8453` (Base), `137` (Polygon), `42161` (Arbitrum). Optional — defaults to `8453` (Base) when omitted. |
| `--contract-address` | `<value>` | — | EVM-style 0x-prefixed 40-hex-char address. |
| `--destination` | `<value>` | — | EVM-style 0x-prefixed 40-hex-char address. |
| `--amount` | `<value>` | — | Amount as a string in the token's smallest unit (e.g. `100000000` for 100 USDC at 6 decimals). |

## `noya chain erc721`

_ERC-721 NFT reads + mints + transfers._

### `noya chain erc721 get-balance`

Auto-generated from sdk.chain.erc721.getBalance

| Flag | Value | Choices | Description |
| --- | --- | --- | --- |
| `--chain-id` | `<value>` | — | EVM chain ID — e.g. `1` (Ethereum), `8453` (Base), `137` (Polygon), `42161` (Arbitrum). Optional — defaults to `8453` (Base) when omitted. |
| `--contract-address` | `<value>` | — | EVM-style 0x-prefixed 40-hex-char address. |

### `noya chain erc721 mint`

Auto-generated from sdk.chain.erc721.mint

| Flag | Value | Choices | Description |
| --- | --- | --- | --- |
| `--chain-id` | `<value>` | — | EVM chain ID — e.g. `1` (Ethereum), `8453` (Base), `137` (Polygon), `42161` (Arbitrum). Optional — defaults to `8453` (Base) when omitted. |
| `--contract-address` | `<value>` | — | EVM-style 0x-prefixed 40-hex-char address. |
| `--destination` | `<value>` | — | EVM-style 0x-prefixed 40-hex-char address. |

### `noya chain erc721 transfer`

Auto-generated from sdk.chain.erc721.transfer

| Flag | Value | Choices | Description |
| --- | --- | --- | --- |
| `--chain-id` | `<value>` | — | EVM chain ID — e.g. `1` (Ethereum), `8453` (Base), `137` (Polygon), `42161` (Arbitrum). Optional — defaults to `8453` (Base) when omitted. |
| `--contract-address` | `<value>` | — | EVM-style 0x-prefixed 40-hex-char address. |
| `--token-id` | `<value>` | — | Token ID to transfer (decimal string). |
| `--destination` | `<value>` | — | EVM-style 0x-prefixed 40-hex-char address. |

## `noya chain morpho`

_Morpho vault deposits + withdrawals._

### `noya chain morpho deposit`

Auto-generated from sdk.chain.morpho.deposit

| Flag | Value | Choices | Description |
| --- | --- | --- | --- |
| `--chain-id` | `<value>` | — | EVM chain ID — e.g. `1` (Ethereum), `8453` (Base), `137` (Polygon), `42161` (Arbitrum). Optional — defaults to `8453` (Base) when omitted. |
| `--vault-address` | `<value>` | — | EVM-style 0x-prefixed 40-hex-char address. |
| `--assets` | `<value>` | — | Asset amount to deposit/withdraw, in the vault's underlying-asset smallest unit. |

### `noya chain morpho withdraw`

Auto-generated from sdk.chain.morpho.withdraw

| Flag | Value | Choices | Description |
| --- | --- | --- | --- |
| `--chain-id` | `<value>` | — | EVM chain ID — e.g. `1` (Ethereum), `8453` (Base), `137` (Polygon), `42161` (Arbitrum). Optional — defaults to `8453` (Base) when omitted. |
| `--vault-address` | `<value>` | — | EVM-style 0x-prefixed 40-hex-char address. |
| `--assets` | `<value>` | — | Asset amount to deposit/withdraw, in the vault's underlying-asset smallest unit. |

## `noya chain pyth`

_Pyth oracle price reads (chain-agnostic)._

### `noya chain pyth fetch-price`

Auto-generated from sdk.chain.pyth.fetchPrice

| Flag | Value | Choices | Description |
| --- | --- | --- | --- |
| `--price-feed-id` | `<value>` | — | Pyth price feed ID — the hex value resolved via `fetchPriceFeed` (e.g. `0xe62df6c8b4a85fe1a67db44dc12de5db330f7ac66b72dc658afedf0f4a415b43`). |

### `noya chain pyth fetch-price-feed`

Auto-generated from sdk.chain.pyth.fetchPriceFeed

| Flag | Value | Choices | Description |
| --- | --- | --- | --- |
| `--token-symbol` | `<value>` | — | Token symbol to resolve to a Pyth price feed ID (e.g. `BTC`, `ETH`, `SOL`). |

## `noya chain wallet`

_Wallet metadata + native (ETH/native token) transfers._

### `noya chain wallet get-wallet-details`

Auto-generated from sdk.chain.wallet.getWalletDetails

| Flag | Value | Choices | Description |
| --- | --- | --- | --- |
| `--chain-id` | `<value>` | — | EVM chain ID — e.g. `1` (Ethereum), `8453` (Base), `137` (Polygon), `42161` (Arbitrum). Optional — defaults to `8453` (Base) when omitted. |

### `noya chain wallet native-transfer`

Auto-generated from sdk.chain.wallet.nativeTransfer

| Flag | Value | Choices | Description |
| --- | --- | --- | --- |
| `--chain-id` | `<value>` | — | EVM chain ID — e.g. `1` (Ethereum), `8453` (Base), `137` (Polygon), `42161` (Arbitrum). Optional — defaults to `8453` (Base) when omitted. |
| `--to` | `<value>` | — | EVM-style 0x-prefixed 40-hex-char address. |
| `--value` | `<value>` | — | Amount of native token (ETH/MATIC/etc.) to send, in whole units. |

## `noya chain weth`

_Wrap / unwrap native token ↔ WETH._

### `noya chain weth unwrap-eth`

Auto-generated from sdk.chain.weth.unwrapEth

| Flag | Value | Choices | Description |
| --- | --- | --- | --- |
| `--chain-id` | `<value>` | — | EVM chain ID — e.g. `1` (Ethereum), `8453` (Base), `137` (Polygon), `42161` (Arbitrum). Optional — defaults to `8453` (Base) when omitted. |
| `--amount-to-unwrap` | `<value>` | — | Amount of WETH to unwrap back to ETH, in wei. |

### `noya chain weth wrap-eth`

Auto-generated from sdk.chain.weth.wrapEth

| Flag | Value | Choices | Description |
| --- | --- | --- | --- |
| `--chain-id` | `<value>` | — | EVM chain ID — e.g. `1` (Ethereum), `8453` (Base), `137` (Polygon), `42161` (Arbitrum). Optional — defaults to `8453` (Base) when omitted. |
| `--amount-to-wrap` | `<value>` | — | Amount of ETH to wrap into WETH, in wei. |

## `noya chain x402`

_Make x402-gated HTTP requests + discover x402 services._

### `noya chain x402 discover-services`

Auto-generated from sdk.chain.x402.discoverServices

| Flag | Value | Choices | Description |
| --- | --- | --- | --- |
| `--query` | `<value>` | — | Free-text filter for x402 service discovery. Omit to list all known services. |

### `noya chain x402 make-http-request`

Auto-generated from sdk.chain.x402.makeHttpRequest

| Flag | Value | Choices | Description |
| --- | --- | --- | --- |
| `--url` | `<value>` | — | Target URL of the x402-gated service. |
| `--method` | `<value>` | — | HTTP method. Default `GET`. |
| `--headers` | `<value>` | — | Extra request headers to send. |
| `--body` | `<value>` | — | Request body for POST/PUT/PATCH. |
